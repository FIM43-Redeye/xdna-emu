# Rust build output
/target

# Local git worktrees (parallel work streams; never tracked)
/.worktrees

# IDE
.idea/
*.swp
*.swo
*~

# Mock XRT build artifacts
mock_xrt/build/

# NPU runner build artifacts
tools/npu-runner/build/

# bridge-trace-runner build artifacts
bridge-runner/build/

# clangd index cache
bridge-runner/.cache/

# validate-readback build artifacts + clangd cache
tools/validate-readback/build/
tools/validate-readback/.cache/

# txn-poll-probe build artifacts + clangd cache
tools/txn-poll-probe/build/
tools/txn-poll-probe/.cache/

# telemetry-probe build artifacts + clangd cache
tools/telemetry-probe/build/
tools/telemetry-probe/.cache/

# aiecc.py per-MLIR project directories (created next to .mlir input/output)
aie.mlir.prj/
aie_traced.mlir.prj/
test-traced-verify.mlir.prj/
*.mlir.prj/

# Captured NPU hardware outputs (machine-specific, regenerate with capture_npu_outputs)
tests/npu-outputs/

# Generated/extracted test files (regenerate from mlir-aie)
tests/mlir-aie-extracted/*

# Validation reports (regenerable)
reports/

# Temporary/scratch files
*.zip
*.tar.gz
fake.xclbin
stdout

# Documentation drafts (not ready for commit)
CURRENT-REFACTOR.md

# Scratch outputs from manual aiecc.py / chess runs at repo root
/Release/
/k*.o
/k*.o.lst
/insts.bin
/aie.xclbin
/raw
/*.rlib
/*.patch
/*.log

# xdna-driver (external dependency, not a submodule -- see patches/)
/xdna-driver

# XRT plugin build artifacts
/xrt-plugin/build/

# Build script output
/build/

# Simulator artifacts
.AIE_SIM_CMD_LINE_OPTIONS
AIESimulator.log
aiesimulator_debug.log
diag_report.log
qemu_rp.log
xcd.log
xsc_report.log

# Temporary extracted/generated artifacts
*.pdi
aie_part.bin
aie_part.json

# Claude Code session state (user-specific)
.claude/session-state.md
.claude/settings.local.json
.claude/skills/
.claude/scheduled_tasks.lock

# Python
__pycache__/
.pytest_cache/
*.pyc

# OS files
.DS_Store
Thumbs.db
