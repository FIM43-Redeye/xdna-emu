---
class: host-firmware-dispatch
subsystem: off-array host + NPU management-firmware launch / dispatch path
posture: off-array -- the wall-clock gap between core-start and the first runtime-sequence DMA dispatch; the real firmware component boots, handles host requests, loads a configured PDI into the shared array, and completes frozen and runtime-relocated dual-compiler kernels through real shim TCTs and the pinned unmodified-driver boundary using chained, direct-CU, and direct-DPU execution envelopes, while broader completion actors and command lifecycles remain open
status: dispatch-latency documented+deferred; core-reset mechanism root-caused, Part 1 landed (`1e9e6700`); management apertures, natural go-alive publication, shared array state, public firmware component FFI, shared BAR4 state, address-derived X2I-tail routing, blocking and asynchronous management DMA, firmware-selected device-heap translation, shared source-76 completion, busy-lane drain acknowledgement, configured PDI handoff, XRT AIE-DDR address translation, and Chess+Peano unmodified-driver round trips through `CHAIN_EXEC_NPU`, direct `EXECUTE_BUFFER_CF`, and observable transaction-ELF `EXEC_DPU` fixed/captured; the unconfigured control still reaches APP_LOAD_PDI_FAIL before the PDI loader; other TCT actors and broader driver command/lifecycle coverage remain pending
---

# Host / Firmware Dispatch Gaps

The wall-clock gap between core-start and the first runtime-sequence-driven DMA
dispatch. Most of this is genuinely **off-array**: the (jittery) time the host +
NPU firmware spend launching and configuring before the array does anything,
which the emulator deliberately does not model (modeling it would inject host
jitter without improving array cycle-accuracy). But the #140 SP-4a investigation
found that *part* of the EMU-side window was an on-array bug: the emulator ran
compute cores at CDO/config time because it ignored the `CORE_CONTROL` reset bit.

**This class is the seam to the firmware-emulation endgame** -- the reset
deassert, the ~8000cy mailbox latency, the dispatch gates, and the core warm-up
are all hardcoded firmware behaviors that the "true simulator" route would
replace by loading real management-processor firmware. See the firmware-dream
memory note and finding
[`2026-07-03-sp4a-core-reset-mechanism-part1.md`](../superpowers/findings/2026-07-03-sp4a-core-reset-mechanism-part1.md).

## Causal-Mechanism Closure Order

Firmware completion is organized by causal mechanisms, not by accumulating
command captures. Each mechanism is a closure gate: finish its complete pinned
NPU1 driver-reachable contract before moving to the next one. Hardware runs are
targeted checks for facts that cannot be derived from the open toolchain or the
signed firmware; captures are supporting receipts, not the deliverable.

1. **Context semantics.** Allocation and column ownership; context identity and
   queue authority; host-buffer and CU state; clean destruction and reuse;
   concurrent allocation and exhaustion; stale/invalid access; busy destruction;
   and every pinned driver-reachable destroy/recreate, timeout, suspend, and
   resume transition. This is the active gate.
2. **Complete TCT publication.** Replace the shim-S2MM0-channel-0 proof slice
   with the toolchain-derived actor map and completion routing required by all
   supported array work.
3. **Asynchronous errors.** Go beyond initial event-buffer registration to
   causal error production, DMA payload delivery, acknowledgement,
   re-registration, and recovery-visible state.
4. **Multi-context/PASID isolation.** Select mappings by their real ownership
   keys and prove that contexts cannot alias or mutate one another's host or
   device views.
5. **PSP/SMU power, reset, and clocks.** Make the existing functional frontend
   transitions causally control firmware and array state instead of merely
   recording accepted commands.
6. **Clock domains and timing.** Derive firmware, management-DMA, controller,
   and array scheduling relationships so externally observable timing emerges
   from modeled causes rather than fixed mailbox-delay constants.

| Gap | Model vs hardware | Where | Status / rationale |
|-----|-------------------|-------|--------------------|
| Dispatch latency (core-start -> first drain dispatch) | HW **~25-32k cy** (mean ~30.5k, +/-16%, occasional ~40k) + **~112cy per runtime-sequence instruction** (controller processing rate) + scales with CDO size; EMU **~673cy**. The ~30k base is host->NPU launch + firmware startup (jittery -> off-array). | No emulator model -- deliberately un-modeled off-array latency; would sit in the controller/dispatch path (`src/npu/`) if ever added. Evidence: `build/experiments/pathA-cntr-spike/w8-drainpace/` (6 in-core cntr runs, dispatch swung 24.7k-40.7k cy). | **DOCUMENTED, deferred (2026-07-03, #140 SP-4a-discovered).** **Orthogonal to the SP-4a oracle**: the array-side fill is exactly deterministic (consA fills to **exactly 5** every run despite dispatch swinging 24.7k-40.7k cy), so the drain-pacing work is unaffected. Not yet decomposed into host-launch vs firmware-config vs per-instruction-controller cost -- defer to a dedicated marker-probe experiment. Finding: [`2026-07-03-sp4a-fill-reconciliation-drain-pacing.md`](../superpowers/findings/2026-07-03-sp4a-fill-reconciliation-drain-pacing.md). |
| Firmware go-alive publication (real-firmware route) | The unmodified firmware now boots naturally to scheduler `waiti`, builds the `_NPU` management descriptor in local SRAM, exposes it through firmware-configured I2X slot 13 at `0x030bb000`, and publishes that device address through reset-preconfigured I2X slot 15 / `FW_ALIVE_OFF` at `0x030bf000`. Driver-style clearing of the alive word reaches the same local SRAM backing. | `src/firmware/mod.rs` (`try_load_m2c`, `boot_to_idle_with_device`), `src/firmware/mmio.rs` (`host_sram_load32` / `host_sram_store32`), and `crates/xdna-emu-ffi/src/firmware.rs`; guards `m2c_boot_publishes_alive_state_through_host_sram`, `m2c_boot_with_device_borrows_and_preserves_array_state`, and `public_ffi_boots_pinned_firmware_and_exposes_genuine_alive_state`. | **PUBLIC COMPONENT SEAM FIXED; OUTER TRANSACTION CAPTURED (2026-07-27).** Publication is firmware-produced and externally visible without a shadow structure; the FFI accepts explicit image bytes, boots against the engine's sole `DeviceState`, and exposes genuine SRAM aliases. One real telemetry request produced exactly one matched tail/IRQ/worker/head sequence with zero lost trace samples. The raw FFI is still not itself driver evidence; the separate vfio-user gate now supplies that proof. |
| Phoenix mailbox/controller and management-DMA seam | Host and firmware share the published BAR4 words. X2I tails follow `0x030d0000 + channel * 0x2000`; the firmware handler maps channel `n` to controller source `n + 0x20`. Host tail writes therefore select source 37 for context channel 5 and source 46 for management channel 14, while adjacent X2I-head and I2X words remain inert. After context delivery, firmware uses one of three global management-DMA lanes at `0x27271000 + lane * 0x1000`; its 60-slot translation table maps the internal descriptor endpoint to registered `HostMemory`. | `src/firmware/phoenix_mailbox.rs`, `src/firmware/management_controller.rs`, `src/firmware/mmio.rs`, `src/firmware/xtensa/interp/mod.rs`; guards `host_x2i_tail_asserts_address_derived_management_source`, `host_non_x2i_mailbox_writes_do_not_assert_management_source`, `m2c_first_pinned_startup_command_reaches_firmware_response`, and `m2c_unconfigured_cu_fails_before_pdi_loader`. | **FUNCTIONAL LIFECYCLE FIXED THROUGH THE REAL MODE-3 RESPONSE (2026-07-28).** Mode 0 copies the 112-byte host slot to local `0x00096000`; mode 3 then stages the complete 16 KiB command-chain window at local `0x0007d000`, publishes zero result and cleared busy state, and raises the shared level on source 76. The real firmware handler drains `0xbc000000`; the empty sentinel deasserts source 76 without disabling it. The unconfigured negative control consumes X2I, rejects CU index 0, and publishes `INVALID_PARAM, 0, APP_LOAD_PDI_FAIL` before entering the PDI loader. Sources `56..59` and the `0x3f` L2 mask are AIE error-network maintenance, not this completion path. A `+0x114` drain request sets command bit 1 and suppresses late completion. Measured latency, nonzero hardware results, sources `77..79`, and broader driver lifecycles remain open. |
| Phoenix device-heap / host-buffer translation | The open driver gives firmware device BO addresses in `0x04000000..0x08000000`, while `MAP_HOST_BUFFER` supplies a distinct host context-heap base. The pinned firmware writes the selected record's byte size at `+0x104` and management-DMA view at `+0x108`; its transaction parser later dereferences the unchanged device address through that record and the existing 60-slot translation table. Treating the device address as host identity memory returns the wrong bytes. | `src/firmware/mmio.rs` (`device_memory_host_target`) and guard `attached_device_memory_follows_firmware_selected_host_mapping`; the frozen guards deliberately place device addresses at `0x040...`, their host backing at `0x600...`, and data BOs at `0x640...`. | **FIXED AND DRIVER-REACHABLE (2026-07-29).** The shared CPU load/store route follows the unique firmware-selected record, rejects ambiguous records instead of silently choosing identity memory, and retains identity fallback only when no record applies. Frozen Chess and Peano both pass through guest XRT, the unmodified pinned driver, vfio-user, unmodified firmware, array execution, MSI-X completion, and zero-status context destruction. Evidence and exact tuple are in [`firmware-array-plugin-wiring.md`](../arch/firmware-array-plugin-wiring.md). Multi-context/PASID alias selection remains open. |
| Phoenix clean context destruction and reuse | The six `0xe740 + fw_ctx_id*0x1b8` entries are firmware-context slots, not column records. Clean `DESTROY_CONTEXT` tombstones the selected slot and removes its management-DMA translation while leaving its prior size/view words inert; the old device address no longer resolves to the destroyed heap. The next matching `CREATE_CONTEXT` deterministically reuses that firmware ID, and a new `MAP_HOST_BUFFER` selects only the replacement heap. | Signed-firmware guard `m2c_clean_destroy_fully_reclaims_context`; canonical driver telemetry indexes its six-entry map by `fw_ctx_id`. | **CHARACTERIZED (2026-08-01).** No shadow context state or production behavior was needed: the signed firmware and existing management-DMA table already implement clean reclamation. Concurrent allocation/exhaustion, stale IDs, busy destruction, and restart failure remain open. |
| Phoenix `CONFIG_CU` / PDI / first completion handoff | The open driver passes registered device-heap addresses in `0x04000000..0x08000000` unchanged and configures the firmware's translated management-DMA view. With a real xclbin PDI registered through `CONFIG_CU`, the pinned firmware reads that PDI through both views, executes its own loader, and emits direct and DMA-backed CDO writes through its `0x84000000` transaction array view. That view and the `0x9c000000` management view address the engine's sole `DeviceState`; the completed shim S2MM0 task returns through source 76 and the shared completion aperture. | `src/firmware/image.rs`, `src/firmware/mmio.rs`, `src/firmware/runtime.rs`, and guards `m2c_configured_cu_executes_frozen_chess_kernel_through_firmware_response`, `m2c_configured_cu_executes_frozen_peano_kernel_through_firmware_response`, and `m2c_configured_cu_executes_frozen_chess_kernel_through_direct_firmware_response`; design records [`2026-07-28-phoenix-config-cu-pdi-handoff-design.md`](../superpowers/specs/2026-07-28-phoenix-config-cu-pdi-handoff-design.md), [`2026-07-28-phoenix-shim-tct-firmware-handoff-design.md`](../superpowers/specs/2026-07-28-phoenix-shim-tct-firmware-handoff-design.md), and [`2026-07-29-phoenix-direct-execute-buffer-cf-design.md`](../superpowers/specs/2026-07-29-phoenix-direct-execute-buffer-cf-design.md). | **FIRST DRIVER-REACHABLE ROUND TRIP FIXED THROUGH BOTH ENVELOPES (2026-07-29).** `$PS1` signed extent includes the fixed `0x100`-byte header plus its body-size field. The genuine loader leaves each frozen PDI unchanged, populates program and data memory only in assigned physical column 1, and configures one core. Both Chess and Peano `add_one_using_dma` artifacts produce output `2..=65`; their shim S2MM0 tokens drain as the observed `0x0020600f` record, after which unmodified firmware consumes X2I and publishes successful responses through both the default `CHAIN_EXEC_NPU` and direct `EXECUTE_BUFFER_CF` paths of the pinned unmodified driver. Other actors, commands, and timing remain open. |
| Phoenix transaction-ELF shim DDR address view | Open XRT `shim_dma_48` relocation writes `existing addend + CPU BO address + 0x80000000` into each 48-bit shim BD. Unmodified firmware stages that AIE/NoC-visible address unchanged. The prior direct executor subtracted the bias privately, but firmware-driven execution bypassed it; shared shim DMA then read and wrote the biased address as CPU `HostMemory`, yielding an all-zero output despite a successful response. | `src/device/dma/engine/stepping.rs`, `src/npu/executor.rs`, and guard `m2c_configured_cu_executes_pinned_chess_elf_through_direct_dpu_response`; approved and verified design [`2026-07-29-phoenix-observable-transaction-elf-design.md`](../superpowers/specs/2026-07-29-phoenix-observable-transaction-elf-design.md). | **FIXED AND DRIVER-REACHABLE (2026-07-29).** The shared shim-DMA boundary resolves XRT's AIE DDR alias to the registered CPU mapping and preserves exact host-view mappings for non-ELF streams; the executor-only rewrite is removed. Targeted bridge validation passed both variants on physical hardware and the emulator, and both pinned KVM tuples produced `42..=105` through normal XRT, unmodified driver, unmodified firmware, direct `EXEC_DPU`, MSI-X, and matched teardown. Evidence: Chess `20260729T210239Z-3454415`; Peano `20260729T210520Z-3461755`. Full ELF/PDI bundling, preemption, recovery, and timing remain open. |
| Phoenix management apertures shadowed by local DRAM | The open driver defines the contiguous NPU1 device aperture `0x03000000..0x03100000` (BAR0/BAR2/BAR4). It must preempt the broad low D-side local-memory view, including for bulk fills that cross either boundary. | `src/firmware/mmio.rs` (`region`, `data_load*`, `data_store*`, `data_fill`); authoritative map `../xdna-driver/src/driver/amdxdna/npu1_regs.c`. | **FIXED (2026-07-26, `6665d01f`).** Device-aperture routing now wins before local-memory fallback, with boundary-splitting covered by `phoenix_device_aperture_preempts_the_low_local_view`; natural firmware stores now reach the device backing used by the SRAM-alias model. |
| Core reset-deassert at launch (`CORE_CONTROL` reset bit) | EMU derived core run-state from the ENABLE bit alone and ran cores at **CDO time (cyc 0)**; silicon holds each core `CORE_CONTROL=0x03` (ENABLE=1, RESET=1 = enabled-but-held) until **firmware deasserts reset at kernel launch**. Nothing in the CDO or `insts.bin` clears the reset bit -- it is a pure firmware side effect. So EMU cores ran ~673cy ahead of the shim dispatch, filling the objectfifo pipeline HW keeps cold. Proven by a **1-second-invariant post-load-sleep HW sweep** (pipe cold at dispatch regardless of ~1e9-cycle pre-launch). | `src/device/state/compute.rs` (run-state now `enabled = ENABLE & !RESET`); `DeviceState::release_core_resets()` (the firmware-launch seam); [finding: 2026-07-03 core-reset mechanism](../superpowers/findings/2026-07-03-sp4a-core-reset-mechanism-part1.md) | **ROOT-CAUSED, Part 1 landed (`1e9e6700`, byte-identical, 3585 lib tests).** Part 1 makes the reset bit real and names the firmware seam (called at config-completion for now -> behavior unchanged). **Part 2 = firmware-launch TIMING**: relocate `release_core_resets()` off config-completion to the true launch point; couples to an unmeasured HW fact (config-register MMIO cost in AIE-cycles) -- pin with a runtime-sequence padding HW probe. This root cause **supersedes** the prior drain-throttle-as-primary framing for the SP-4a offset (see the send-cadence residual in [`dma-stream-resources.md`](dma-stream-resources.md)). Endgame: real firmware on a simulated management processor. |
