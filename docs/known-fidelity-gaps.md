# Known Fidelity Gaps

A single registry of **confirmed** points where the emulator (or the aiesim
oracle) provably disagrees with real NPU hardware. Each is documented in detail
elsewhere; this page is the index, so the pattern is visible in one place and we
don't re-investigate the same gap twice. (That happened once already: a stale
"shim queue is 8-deep" caveat sent a whole session chasing a dead mechanism.)

**Scope:** confirmed HW-disagreement gaps only -- behaviors where we have ground
truth that the model is wrong. This is *not* the list of not-yet-implemented
features (vector compute semantics, stream-switch per-port types, micro-timing);
those live in [`toolchain-sources.md`](toolchain-sources.md) and the
[roadmap](../ROADMAP.md).

**How these surface:** mostly via the aiesim oracle (the XRT-plugin -> aiesim
path). The count rising is the oracle doing its job -- before it, these were
invisible. Each was a deliberate "document, don't chase" call; see the per-gap
rationale.

---

## Emulator (xdna-emu interpreter)

These cluster in **one subsystem: shim DMA finite-resource modeling**. Shared
root posture: our DMA model is *optimistic where silicon is strict* -- it
assumes generous/infinite resources (deep queues, unbounded token buffers,
freely reusable BDs) where the hardware is finite and will stall or wedge. All
are **low real-workload impact**: real compilers don't hit these; they bite
synthetic / pathological kernels (e.g. our own calibration sweeps). The one
toolchain-derivable member (task queue depth) is now **fixed**; the two
residual gaps both need **HW-empirical data** the open-source toolchain does not
expose, so they stay documented-and-deferred rather than guessed.

| Gap | Model vs hardware | Where | Status / rationale |
|-----|-------------------|-------|--------------------|
| TCT token buffer | we model **unbounded**; HW has finite token backpressure that stalls the channel (`Stalled_TCT`) | `src/device/dma/token.rs` (`TokenState`; the deliberate simplification is called out in the doc comment) | **OPEN.** Documented in code. aie-rt exposes only a 1-bit `STALLED_TCT` flag, **no numeric depth** -- faithful finite modeling needs an empirically-measured HW depth (HW-gated; a guessed depth would violate derive-from-toolchain). |
| BD reuse-while-live / BD-pool over-allocation | we re-parse a BD on reuse and complete; HW wedges when a kernel needs > 16 distinct shim BDs (in-flight BD reuse) | [finding: 2026-06-06 shim BD-pool over-allocation](superpowers/findings/2026-06-06-shim-bd-pool-overallocation-nonmonotonic-wedge.md) | **OPEN (won't-fix).** The HW wedge is *non-monotonic* in K (k8 pass, k9 wedge, k12 pass, k16 wedge) -- no clean model. Generator (`gen-shim-chain-sweep.py`) capped at K=8 instead. |

A second, unrelated cluster: **trace held-level encoding**. After the held-level
re-architecture (LOCK_STALL / PORT_* / DMA stall-starvation / core stall family
emit as B..E spans, decoded by upstream `parse_trace`), the span *structure* is
faithful but two residual gaps remain. Both are **timing/encoding artifacts, not
mechanism bugs**, and both are stable Phoenix->Strix (DMA delivery timing, not
silicon-version-specific). Measured on `vec_mul_trace_distribute_lateral` (peano,
interpreter backend) vs real NPU1.

| Gap | Model vs hardware | Where | Status / rationale |
|-----|-------------------|-------|--------------------|
| Held-level falling edge, close-to-empty | a held span whose deassert lands on a cycle with no *other* concurrent frame cannot encode its falling edge in mode-0 (no empty frame); the close defers to the next frame / end-of-segment. HW closes the span at the deassert cycle. Visible as the mem-trace `DMA_S2MM_*_STREAM_STARVATION` span staying open to EOS. | `src/device/trace_unit/mod.rs` (`set_event_level` / `commit_pending_frame`, the `active==0` defer); [finding: 2026-06-08 falling-edge depends on concurrent levels](superpowers/findings/2026-06-08-lock-stall-falling-edge-depends-on-concurrent-levels.md) | **DOCUMENTED.** Matches how HW closes a level span (no synthetic empty frame); the residual is only the *lone-level* case where no concurrent frame carries the close. With the per-cycle TRUE metronome the deferral is bounded to ~1 cycle. |
| Held-level count under-emission (DDR-fill / bursty delivery) | our stream/DMA delivery is *smoother* (more continuous) than HW's bursty ~1024-cycle bursts, so level events assert fewer times: `LOCK_STALL` ~11 vs 19 spans, `PORT_RUNNING_0` 3 vs 6, `DMA_S2MM_*_STREAM_STARVATION` 2 vs 4. (Counts are span B-phases; the held mechanism itself fires correctly -- see the single 943-cycle hold in the finding.) | DMA/stream delivery timing model; [finding: 2026-06-07 LOCK_STALL over-emission](superpowers/findings/2026-06-07-lock-stall-overemission-interp-vs-hw.md), [finding: 2026-06-08 falling-edge](superpowers/findings/2026-06-08-lock-stall-falling-edge-depends-on-concurrent-levels.md) | **DOCUMENTED.** Held-level re-architecture fixed the gross ~375x LOCK_STALL over-emission; the residual count gap is a DMA fill-latency/burstiness axis, the same axis as the aiesim "trace stall/starvation micro-timing" row below. Calibrate against HW, not chase in the trace encoder. **UPDATE (2026-06-15, #140) -- SUPERSEDED, see 2026-06-16.** The "shim host-DDR burst-delivery jitter" narrative and the seeded-PRNG `AIE2_DDR_PHOENIX` model that "band-matched within 1 sigma" were a **metric artifact**: the cadence tool counted trace *frame-records* (the re-checkpoint frames emitted at every concurrent edge), not *spans*, manufacturing run-to-run variance that does not exist on silicon. **CORRECTED (2026-06-16, commit `ace4c321`+):** measured span-based (`tools/port-span-cadence.py`, oracle B/E, idle-gap>2), NPU1 `add_one_using_dma` `PORT_RUNNING` is **deterministic** (std 0, 15 HW runs): HW `[slot0=1, slot1=5, slot4=3, slot5=4]`. Default EMU (phoenix removed) is `[1, 6, 5, 4]` -- **slot0 and slot5 (the shim-DDR-gated ports) match exactly**; only slot1 (+1) and slot4 (+2), the two **compute-core-gated** ports, diverge. So this is **not** a DDR-delivery gap; only the compute-core-gated ports diverge. **CORRECTED AGAIN (2026-06-16 cont.) -- the "VLIW ILP under-modeling" reading was itself WRONG.** A code+ISA scout (two Explore passes + chess-ELF disasm) found: (1) EMU inserts **zero load-use/hazard stalls** -- the `HazardDetector` is dormant, `check_*` never called in the execution path (grep-verified); (2) EMU per-bundle cost = 1cy (`record_instruction(1)`) + 1cy/lock-txn (`record_stall(1)`, cycle_accurate.rs:955), with load latency as a *deferred-write only* -- so EMU **counts compiled bundles at HW's 1-bundle/cycle issue rate**, nothing more; (3) the compute loop is **SCALAR, Chess-hand-software-pipelined** (`main_core_0_2.elf` `core_0_2`: 8 `lda` then 8 `st`+`add.nc #1` per 8-word buffer; loads issued up front so the 7cy latency is covered -- EMU's deferred-write reproduces it), NOT a vectorized add where forwarding matters; (4) the flat ~64-71cy/buffer is **lock function-call overhead** -- 4 acquire/release `jl` calls/buffer x ~13cy (5+5 branch-delay slots each) ~= 52cy + ~16cy load/store, and **HW runs the identical binary so it pays the same overhead**. There is no "core runs the add-loop faster" -- most of the period is branch-delay slots. RULED OUT: output path (`out1_prod` never 0, `out1_cons` always 0), beat-vs-held encoding (slot0/slot5 match), DMA/stream FIFO depths (HW-faithful 4/4/2 AM020), and now **hazard/ILP under-modeling (no such stalls exist)**. The stochastic DDR (`AIE2_DDR_PHOENIX`) model is **removed** (`burst.rs` deleted). aiesim is NOT a usable oracle (PORT_RUNNING-silent). **Corrected divergence candidates** for the ~47cy-longer EMU compute extent (span 284 vs 237): **Lever A** = the +1cy/lock-arb charge -- but this was HW-CALIBRATED (commit `737f5505`, see the "Trace stall/starvation micro-timing" row: tenant-4 core loop 2125->2129 = HW exact, corpus clean), so it is probably correct, though worth re-checking it isn't double-applied in the 4-lock-per-buffer case; **Lever B (prime suspect)** = lock-acquire stall duration driven by *when the DMA delivers/releases buffers* (stream/FIFO coupling -- same axis as this row's original "DMA fill latency" framing and the trace-row source 2). **A/B MEASURED (2026-06-16 cont.2, `build/experiments/gap140/ab_split.py`):** Lever A (lock-arb) is DEAD -- EMU traces with the charge OFF vs ON are byte-identical, so it does not touch the port cadence. **Port directions NAILED by decoded-trace causality** (`build/experiments/gap140/ab_timeline.py`): on a shared timebase the pipeline fires in dataflow order in BOTH worlds -- `PORT_0(recv<-shim)@0 -> PORT_4@27-35 -> PORT_1@113-129 -> PORT_5(send->shim)@last`. A core cannot output before it ingests input, so PORT_4 (fires first) = **send->compute (input)** and PORT_1 (later) = **recv<-compute (output)**. MLIR confirms (`flow(mt DMA0->compute)`, `flow(compute DMA0->mt DMA1)`). The 2026-06-14 FSM-probe labels (port1=send/port4=recv) were WRONG -- the in-process probe misleads for DIRECTIONS too. Final: slot0=recv<-shim, slot1=recv<-compute, slot4=send->compute, slot5=send->shim. Only **slot4 (send->compute, INPUT delivery)** diverges (5 vs HW 3); slot0/5 match, slot1 (output) +1. The `1->5` swing was `5675bdd5` (beat-crossing PORT_RUNNING), so the old `slot4=1` was a stale-semantics artifact. The decoded oracle shows BOTH EMU and HW emit **5 raw input-beat-runs**; the difference is the gap pattern -- HW *clusters* the opening 3 runs (gaps 1,1 -> continuous `[0,94]`) then settles ~50-56cy; EMU *spreads* the opening (gaps 27,40) then matches steady-state. (The "HW ~8-9 pulses" that suggested EMU under-emits was the in-process `cycle_beat` probe, not the decoded oracle -- discard it.) So #140 is now: **memtile->compute INPUT-delivery opening transient** -- HW streams the opening to fill BOTH initially-free compute input slots (producer lock init=2) back-to-back; EMU sends one 8-word buffer, waits ~27cy for the core to free a slot, then the next (doesn't exploit the input double-buffer depth at the opening). Steady-state matches. NOT lock-arb, NOT core-ILP; the core's instruction timing is faithful -- the gap is how the memtile FEEDS the core at the opening (memtile MM2S-0 -> compute S2MM-0 delivery/lock-acquire timing, overlaps #132). **FIX LANDED (2026-06-16, commit `67987db3`): stream egress metered at the AXI4-Stream rate (1 word/cyc), not the 4-word data bus.** The MM2S was bursting `words_per_cycle=4` (the tile data MEMORY bus) into the shallow stream FIFO; the AIE2 inter-tile stream is 32-bit = 1 word/cyc (XFORM probe: memtile MM2S `ch6` ~4 w/cyc vs compute S2MM `c2ch0` ~1 w/cyc). New data-driven `stream_words_per_cycle=1` selects rate by narrowest interface crossed. Corroborated: 3 chained-BD tests asserted 4 w/cyc but cited HW `on16 off1` (= 1 w/cyc) -- fix makes them match HW interval 17 for the first time. **Result PARTIAL:** slot4 opening 33->46cy (toward HW 94), first stall 27->16cy, steady-state preserved; but slot4 still 5 sub-bursts vs HW 3, and slot0 shifted 1->2 (structurally toward HW's 2-span, gap 7 vs HW 1). **Residual (open):** the egress rate was *a* factor, not the whole opening transient -- the cap at 46cy is the shim->memtile->compute 2-stage RELAY FILL (memtile forwards only what the shim delivered), the next target. Spec `superpowers/specs/2026-06-15-ddr-stochastic-delivery-jitter.md` superseded; finding: `superpowers/findings/2026-06-16-port-cadence-metric-was-frame-records.md`. **HW DISAMBIGUATION + REAL GAP FOUND (2026-06-16 cont.3, commit `a02187e4`).** Ran the decisive HW experiment -- added `PORT_STALLED_4` (+0/1/5) to the memtile capture (`XDNA_TRACE_MEMTILE_EVENTS` override, read by emu-bridge-test.sh) and decoded on real NPU1. **Verdict: PORT_RUNNING semantics are ALREADY CORRECT.** On HW the memtile send-port `PORT_RUNNING_4` and `PORT_STALLED_4` are **perfectly complementary** -- every RUNNING gap is exactly a STALLED interval; HW's RUNNING *drops* during backpressure just like EMU's per-beat model. The "HW holds RUNNING through stalls" hypothesis is REFUTED; the slot4 5-vs-3 sub-burst residual is a small opening-timing detail (EMU stalls ~4-10cy earlier), not a semantics bug. **The experiment exposed a different, concrete gap: EMU emitted ZERO PORT_STALLED on circuit-routed DMA ports** (`cycle_stalled` was set only in the packet-route path; circuit routes -- most inter-tile DMA, incl. memtile MM2S->compute -- skipped it silently). **FIXED (`a02187e4`):** `StreamSwitch::mark_stalled_ports()` runs once post-routing -- `cycle_stalled |= has_data() && !cycle_beat` (a port holding data that did not beat this cycle was backpressured). Evaluated after all routing so it stays exclusive with PORT_RUNNING (0 overlapping cycles, per-cycle probe verified). EMU now reproduces HW's complementary RUNNING/STALLED tiling. `cycle_stalled` is trace-only -> data/cycle-counts/PORT_RUNNING unchanged, default corpus (PORT_RUNNING-only capture) untouched; 3499 lib tests green. Oracle: `build/experiments/gap140/disambig_stalled.py`. Residual: the minor opening-timing difference (one extra RUNNING burst) -- a DMA double-buffer-fill detail, steady-state matches HW. **CONFIRM-FIRST: the double-buffer-overlap reading is DISPROVEN (2026-06-16 cont.4).** The prior framing above ("EMU sends one 8-word buffer, waits ~27cy, then the next -- doesn't exploit the input double-buffer depth at the opening") is WRONG. Per-cycle XFORM probe (`xform_probe_exclfix.log`): at the first slot4 stall (cyc 7981), compute `objFifo_in1_cons_prod_lock` (`c2lk[0]`, init=2) is already **0** -- the memtile MM2S0 had streamed into **both** compute input buffers before backpressuring (prod 2->1 at ~7947, 1->0 at 7954, stall at 7981 with prod=0). EMU *does* exploit the double-buffer depth; the burst width is not gated by buffer count, because the core consumes **concurrently** (`c2lk[1]` cons-lock and `c2lk[2]` out-prod both tick during the burst -- the kernel is running and producing output by cyc 7967). The "10 ~= 8+2 vs 19 ~= 16+3" arithmetic was a coincidence. **The real residual is consumer-drain CADENCE.** Full HW `PORT_RUNNING_4` spans (2 independent persisted silicon runs agree -- `20260614` + `baseline-cycle-only`): durations `8,8,14,61,19,19` -- HW sustains one **61cy contiguous burst** at the opening (cyc 33->94), pipelining nearly the whole 64-word transfer once warm because its core drains buffers fast enough to keep accepting. EMU (`durs 17,16,10,10,10,9,18`) breaks that into ~16/10cy bursts with backpressure stalls when both compute buffers transiently fill. **Maya's reframe (the important one): a consumer-pacing difference implies the CORE buffer-drain timing is mismodeled, not a DMA cosmetic.** This sits in tension with the prior row's scout conclusion ("core instruction timing is faithful") -- that conclusion was about *bundle-issue rate and lock function-call overhead* (both correct), NOT about whether the core's drain cadence sustains a continuous downstream stream the way HW's does. The bridge comparator (raw re-checkpoint records, the regression oracle) rates `PORT_RUNNING_4` 8/8 OK within +1cy, so this is below the *threshold* lens -- but byte-identical is the north star and a threshold just hides it. **Status: DOCUMENTED, NEXT-SESSION TARGET.** Direction for resume: instrument the compute core's per-buffer drain cadence (cycles from `in1_cons_cons_lock` acquire -> `in1_cons_prod_lock` release per 8-word buffer) and compare EMU vs HW; the question is whether EMU's core frees input buffers at HW's rate during the opening warmup. Evidence on disk: `build/experiments/gap140/{xform_probe_exclfix.log, disambig_stalled.py}`; HW refs `build/bridge-test-results/{20260614,baseline-cycle-only}/add_one_using_dma.chess.hw/`. **ROOT CAUSE FOUND (2026-06-16 cont.5) -- the core hypothesis is DISPROVEN and the bug is the trace-unit ENCODER.** (1) Core is faithful: measured per-buffer drain ~67cy (`drain_cadence.py`), HW total ~666cy vs EMU ~614cy (EMU slightly *faster*). (2) The "HW never stalls slot4" was an **event-selection artifact** (Maya's catch): the 8-slot memtile trace unit only ever selected `PORT_RUNNING_4`, never `PORT_STALLED_4` (id 96 vs 97). An HW **event sweep** (`tools/trace-sweep.py --tiles 0:1:memtile`, full 161-event memtile sweep in **4.65s** -- HW is the cheap fast oracle, EMU can't validate methodology) put both in batch 12: HW recorded `PORT_RUNNING_4 x4 AND PORT_STALLED_4 x4` -> **HW does stall the port (H-A)**, EMU's STALLED is correct. (3) **The hardware law (all 4 ports, real silicon):** `sum(PORT_RUNNING spans) == words == 64` at 1cy/word -- `RUNNING_0 4x16`, `RUNNING_1 8x8`, `RUNNING_4 =64 frag`, `RUNNING_5 4x16`. **EMU violates it on every port:** `RUNNING_0=71, RUNNING_1=187, RUNNING_4=90, RUNNING_5=156` (e.g. RUNNING_1 durs `[8,8,8,11,76,76]`). (4) **Three-layer localization:** NOT the core/DMA (runtime matches); NOT the `cycle_beat` signal (XEDGE reconstruction: ep1 + ep5 encoder-input sums are EXACTLY 64); NOT the decoder (HW + EMU share `parse_trace`, HW decodes 64). -> A correct 64-cyc signal enters EMU's encoder and a 187-cyc span exits the decoder. **The defect is in the skip-token held-level encoding (`commit_pending_frame`, `src/device/trace_unit/mod.rs:926`; spec `superpowers/specs/2026-06-08-skip-token-held-level-encoding.md`): pairs of RUNNING spans merge across the deassert->reassert gap** (ep1's last four 8cy spans decode as two ~76cy blobs). Secondary smaller issue: slot4 `cycle_beat` reads ~70 vs 64 (mild over-count on the memtile->compute feed, likely the 2-stage relay-fill residual). **Regression oracle (free from the sweep): `sum(PORT_RUNNING)==words` per port.** Status: root cause localized, **fix NOT yet implemented** (shared encoder -> blast radius across all level events; recording before surgery per Maya). Full chain + artifacts: finding `superpowers/findings/2026-06-16-port-cadence-metric-was-frame-records.md` (cont.5); `build/experiments/gap140/sweep-port4-hw/`, `{drain_cadence,be_spans,port4_timeline}.py`. **FIX LANDED (2026-06-16 cont.6) -- the trace-ENCODING layer is now CLOSED.** Four bugs fixed, each TDD'd and verified end-to-end against a config-matched HW pair via the upstream oracle (`build/experiments/gap140/upstream_convert.py`, memtile loc 1,1): (1) decoder `70b25060` -- the in-tree `ours` perfetto span builder merged held bursts (mauled HW too); fixed to upstream two-phase deactivate/activate semantics. (2) encoder `edf72f68` -- a gap-opened *lone* hold emitted `Repeat(D-1)`; HW uses `Repeat(D-2)` (the extra arming frame costs one decoder cycle). (3) **model** `52649b88` -- `cycle_beat` was set on BOTH push and pop, double-counting any word a relay port buffered >=1cy; a stream port watches ONE external AXI interface (slave=push, master=pop), so the raw beat signal now obeys `sum==64` per port exactly (was `RUNNING_0=67, RUNNING_4=70` at the *signal* level -- the cont.5 "secondary 2-stage relay-fill over-count" was this double-count, not a fill-timing effect). (4) encoder `924ad3c0`+`8b82dabf` -- the concurrent-span +2/+3 was same-cycle re-checkpoints: `set_event_level` committed each level edge immediately, so N edges on one cycle emitted N `cyc=0` frames (decoder charges +1/frame; span inflation == count of same-cycle frames inside the span, measured exactly). Fixed by **deferring level commits** into the existing one-frame-per-cycle `commit_cycle`/Phase-3f path (AM020 rule, already used for pulses), coalescing same-cycle edges into one snapshot frame. (A skew-debt model was tried first and over-corrected -- debt paid *inside* a held span shortens it; coalescing removes the surplus at the source, no compensation needed.) **Result: all four memtile PORT_RUNNING ports decode to exactly 64** (slot0 70->64 `[34,16,14]`, slot2 70->64), faithfully reproducing the raw cycle_beat signal -- the encoder is now byte-faithful and the cont.5 "187/156 merge" is gone. **Remaining (layer 2, the only open #140 item): burst STRUCTURE of the raw signal itself** -- EMU `slot0 [34,16,14]` vs HW `[16,16,16,16]`, the shim->memtile relay-FILL delivery timing (a model-timing axis, not encoding). Regression oracle stands: `sum(PORT_RUNNING)==words` per port. All four fixes trace-only (data/cycle-counts/RUNNING-STALLED exclusivity unchanged); `--lib` 3504 green. Findings: `build/experiments/gap140/{ENCFIX-FINDINGS.md, EFFECT2-SAMECYCLE-FINDING.md}`. |

The core stall family conversion (MEMORY_STALL / STREAM_STALL / CASCADE_STALL as
held levels) is **not exercised by `distribute_lateral`** -- its trace config does
not include events 23/24/25 -- so those three have unit coverage but **no HW-trace
validation yet**; pending a cascade/stream-stall kernel capture. Two related
modeling notes (not HW-confirmed disagreements, hence not rows above): mode-1
(EventPc) PC anchoring is dropped for stall held levels (the level path carries no
PC, matching the LOCK_STALL precedent), and the executor-level *intra-bundle*
structural bank conflict stays a bounded MEMORY_STALL pulse (it resolves within one
executor step, so a same-step rising+falling would encode as a zero-width span).

A third cluster: **vector-register result visibility (forwarding network)**. The
AIE2 per-operand bypass/forwarding matrix is implemented and validated against
the chess bridge sweep (89/89 PASS, 0 regressions) **and against real Phoenix
(NPU1) silicon**: `vec_mac_bf16`, `two_col`, and all three
`matrix_multiplication_using_cascade` variants (plain/buffer/cascade) PASS
HW==EMU on the bridge (2026-06-09), with `vec_mac_bf16` and `two_col` also
diffing CLEAN on the trace comparison. The W/X-file forwarding model is therefore
HW-verified for the bf16 matmul class; the remaining gaps below are scoped
deferrals, not unvalidated model.

| Gap | Model vs hardware | Where | Status / rationale |
|-----|-------------------|-------|--------------------|
| Accumulator/CM-domain (`VEC_Bypass`) result visibility | not in the per-operand bypass matrix; modeled via the separate MAC-pipeline-latency path (`ExecutionContext::queue_matmul_accum_write`). The matrix resolves W/X-file results; `VEC_Bypass` (MAC/MUL results into the accumulator file) is deferred. | `src/interpreter/state/registers.rs` (`VectorRegisterFile::visible_at`); `src/interpreter/state/context.rs` (`queue_matmul_accum_write`, carries a `FIXME(bypass-model)`) | **OPEN / deferred.** The MAC-latency path is HW-validated *functionally* — all three `matrix_multiplication_using_cascade` variants PASS HW==EMU on the bridge (2026-06-09), which exercises the accumulator-write path — but not yet at trace granularity (those kernels' TRACE PREP fails for an orthogonal trace-injection reason). Accumulator folding into the per-operand bypass matrix is a noted future extension (see `docs/superpowers/plans/2026-06-09-vector-write-result-latency.md` Scope). **Partially advanced 2026-06-10 (`430d841`):** the `VMOV bml,x` (wide vector->accumulator move) write is now deferred by its def latency via the same `queue_matmul_accum_write` path, after the phase-B `vec_conv_bf16_edge` silicon kernels proved an immediate write corrupts delay-slot `VST.CONV` stores (read freshly-overwritten accumulator -> Inf/NaN garbage). All 10 convert modes EMU==silicon. The full bypass-matrix fold (distinguishing `VEC_Bypass` MAC->MAC from `NoBypass` store consumers) is still the open extension. |
| `source_forward` positional alignment for merged memory operands | `SlotOp::source_forward` aligns exactly for compute ops; may drift for fused memory operands that merge address/data into a single TableGen operand list position | `src/interpreter/bundle/slot.rs` (`FIXME(source-forward-memory-alignment)`); `src/interpreter/decode/operand_extraction.rs` | **DOCUMENTED / benign.** The bypass model only reads vector-register compute sources; memory operands use `NoBypass` or are not vector-reg reads, so this positional drift does not affect result visibility decisions. |

A fourth cluster: **`vadd.f`/`vsub.f` special-value NaN payload** (bf16 *and*
fp32). **RESOLVED 2026-06-11** -- full mechanism and evidence in
[`docs/superpowers/specs/2026-06-11-nan-payload-datapath-regime.md`](superpowers/specs/2026-06-11-nan-payload-datapath-regime.md).

The result NaN payload when an operand is a NaN has **two real silicon
regimes**: a **datapath regime** (dominant) where the payload is the actual
exp-255 mantissa-datapath sum `r`, and a **canonical regime** (rare) that forces
payload 1. The regime is residual float-unit HW state -- global across lanes,
deterministic within a session, but it varies across sessions and survives a
driver reload. The *same dense binary* gave canonical 0xFF81 for all 127 Inf+NaN
payloads in the 12:40 HW capture and datapath payloads (120/127 different) in a
later session. So this is **not** bf16-specific, **not** two-NaN-operand-only,
and **not** in the bf16 narrow -- it is the shared `aie2_acc_fp32_add` datapath
(fp32 shows it identically), and `+Inf + -NaN` is exactly the trigger. The
earlier "two-exp-255-operand significand arithmetic, bf16-only" framing was
wrong; seed 6159 lane 29 is `+Inf + -NaN(0x46)`, not a NaN+NaN pair.

The model now produces the **datapath regime** (Maya's call: it is what the ALU
computes; canonical is a residual-state suppression). One change in
`aie2_acc_fp32_add`: `use_r = (a_nan || b_nan) && !overflow` lets the datapath
sum `r` through instead of zeroing it. Validated **8160/8160** against fresh
dominant-regime NPU1 captures (bf16+fp32, add+sub); `cargo test --lib` 3408/3408;
seed 6159 via EMU now reproduces `0xFF8C`. Seeds 6159/6258 are thereby
re-credited.

| Gap | Model vs hardware | Where | Status / rationale |
|-----|-------------------|-------|--------------------|
| special-value NaN payload: datapath vs canonical regime | HW has two residual-state regimes for `vadd.f`/`vsub.f` NaN-input payloads; we now model the dominant **datapath** regime (payload = exp-255 mantissa sum `r`). HW in the rare **canonical** regime forces payload 1 and would mismatch the payload bits (a HW-state artifact, not a model bug). | `src/interpreter/execute/vector_float.rs` (`aie2_acc_fp32_add`, `use_r` gate). | **RESOLVED (datapath regime) 2026-06-11.** Validated 8160/8160 vs dominant-regime HW. Residual open: *what* flips the regime is uncharacterized (not driver reload); low priority since the payload is functionally dead. See the design note. |

---

## aiesim oracle (AMD's proprietary simulator)

Gaps in the *oracle*, not in our emulator. They bound what aiesim can validate.
Notably, in the first case **our interpreter is the more faithful of the two.**

| Gap | What | Where | Status / rationale |
|-----|------|-------|--------------------|
| AIE2 compute-to-compute shared memory | aiesim does not model neighbour shared-memory / lock handoff between adjacent compute tiles; such kernels deadlock in the sim. Real HW + our interpreter run them. | [finding: 2026-06-06 aiesim AIE2 c2c shared memory](superpowers/findings/2026-06-06-aiesim-aie2-cross-core-shared-memory-limitation.md); [`aiesimulator.md`](aiesimulator.md) | **DOCUMENTED.** AMD's own test suite marks this `XFAIL`. Exclude this kernel class from the aiesim oracle; our interpreter is the oracle here. |
| Control-read response aliasing | aiesim's cluster model reused one control block across all beats of a control-packet read response, corrupting the header -> never route-matched -> S2MM starved | [`aiesimulator.md`](aiesimulator.md) (Known Issues) | **RESOLVED locally** via a distinct-object-per-beat clone patch (`ss_probe`, gated by `XDNA_CLONE_BEATS`). Local-only; never shipped. |
| Cross-domain broadcast skew (timer-reset propagation) | aiesim **cannot oracle** the BROADCAST_15 timer-reset propagation skew between timer domains: the cluster model leaves the inter-tile broadcast network unwired (array EventBroadcast units dormant), so our `aiesim-bridge/src/bcast_bridge.cpp` *reconstructs* the flood by injection -- "one row per posedge" by construction. Any cross-domain skew aiesim reports is our injection cadence reflected back (circular). | `aiesim-bridge/src/bcast_bridge.cpp`; `build/experiments/bcast-bridge/FINDINGS.md` | **DOCUMENTED (oracle bound).** This skew is undeterminable from traces in general (one equation, two unknowns; no globally-simultaneous traceable signal) -- full characterization in [`trace/cross-domain-skew-limit.md`](trace/cross-domain-skew-limit.md). Reproduction does not need it; cross-domain causal *attribution* does, and no trace-based oracle (ours, aiesim, or AMD's) can supply it. Do not use aiesim for cross-domain timing calibration. |
| Trace stall/starvation micro-timing | aiesim trace counts of *timing-sensitive* events diverge from HW on `vec_mul_trace_distribute_lateral` (`LOCK_STALL` 3 vs 19, `DMA_S2MM_0_STREAM_STARVATION` 3 vs 2, `PORT_RUNNING_1` 4 vs 5). Per-event timeline analysis decomposes the gap into **three separable sources** (not one blanket "approximate"): **(1) Compute region is cycle-EXACT** -- the user trace markers `INSTR_EVENT_0`->`INSTR_EVENT_1` span **12297 ns in both worlds, all 4 invocations**, to the ns. **(2) DMA/DDR input fill latency is under-modeled** -- first `EVENT_0` at HW 8326 ns vs aiesim 2195 ns (~6131 ns optimistic); it is a **constant offset, not per-iteration drift** (each invocation stays exactly 12297 wide). **(3) The `LOCK_STALL` count gap is the HW per-lock-transaction arbitration cycle** -- both worlds issue the *identical* lock ops (`LOCK_ACQUIRE_REQ`=9/9, `LOCK_RELEASE_REQ`=9/9), but HW trails **every** request with a 1 ns `LOCK_STALL` pulse (uncontended lock still costs 1 arbitration cycle) while aiesim models uncontended acquires as zero-stall. HW 19 = 1 genuine startup wait + 18 per-transaction pulses; aiesim 3 = genuine blocking waits only. | trace bridge commit `7d93a83`; HW-vs-aiesim comparison artifacts in local `build/experiments/bcast-bridge/` (FINDINGS.md, trace_{aiesim,hw}.json) | **DOCUMENTED / expected.** From aiesim's ISS, not our trace bridge. **Calibration rule** (which knobs aiesim is safe to teach the emulator): compute-region cycles -> **safe** (exact); DMA/DDR fill latency and per-lock-op stall cost -> **calibrate against HW only** (aiesim is optimistic/zero there). aiesim is a faithful oracle for trace *structure/flow* and compute-region timing, not for fill latency or lock-arbitration overhead. **UPDATE (2026-06-14, commit `737f5505`):** the per-lock-transaction arbitration cost (source 3) is now MODELED in core *cycle* timing -- every completed acquire/release charges +1 cycle (`cycle_accurate.rs`), calibrated to HW (tenant-4 core loop 2125->2129 = HW exact; full bridge corpus clean, 0 trace divergence). What remains open is the *trace-event* half: emitting the per-transaction `LOCK_STALL` pulse on the uncontended path (the `LOCK_STALL` count gap of this row and the "Held-level count under-emission" row above). Cycle cost closed; trace-pulse emission pending. |

---

## Maintenance

When a new confirmed HW-disagreement surfaces: add a one-line row here pointing
at the detailed finding, and note whether it's fixed, documented-and-deferred,
or won't-fix. Keep this page an index -- detail lives in the linked findings.
