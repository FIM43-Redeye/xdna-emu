diff --git a/src/interpreter/bundle/mod.rs b/src/interpreter/bundle/mod.rs
index 1234567..abcdefg 100644
--- a/src/interpreter/bundle/mod.rs
+++ b/src/interpreter/bundle/mod.rs
@@ -292,6 +292,11 @@ pub fn disassemble_slot(slot_op: &SlotOp) -> String {
         Operation::ScalarEq => "eq",
         Operation::ScalarNe => "ne",
         Operation::ScalarSel => "sel",
+        Operation::ScalarDiv => "divs",
+        Operation::ScalarDivu => "divu",
+        Operation::ScalarMod => "mod",
+        Operation::ScalarSelEqz => "seleqz",
+        Operation::ScalarSelNez => "selnez",

         Operation::VectorAdd { element_type } => {
diff --git a/src/interpreter/bundle/slot.rs b/src/interpreter/bundle/slot.rs
index 1234567..abcdefg 100644
--- a/src/interpreter/bundle/slot.rs
+++ b/src/interpreter/bundle/slot.rs
@@ -280,6 +280,16 @@ pub enum Operation {

     /// Conditional select: dst = cond ? src1 : src2
     ScalarSel,
+    /// Signed integer division: dst = src1 / src2
+    ScalarDiv,
+    /// Unsigned integer division: dst = src1 / src2
+    ScalarDivu,
+    /// Modulo: dst = src1 % src2
+    ScalarMod,
+    /// Select if equal zero: dst = (src2 == 0) ? src1 : src3
+    ScalarSelEqz,
+    /// Select if not equal zero: dst = (src2 != 0) ? src1 : src3
+    ScalarSelNez,

     // ========== Vector Operations ==========
@@ -611,6 +621,11 @@ impl Operation {
             | Operation::ScalarEq
             | Operation::ScalarNe
             | Operation::ScalarSel
+            | Operation::ScalarDiv
+            | Operation::ScalarDivu
+            | Operation::ScalarMod
+            | Operation::ScalarSelEqz
+            | Operation::ScalarSelNez
              => SlotIndex::Scalar0,

             // Scalar multiply (Scalar1 slot)
