diff --git a/src/driver/amdxdna/aie2_message.c b/src/driver/amdxdna/aie2_message.c
--- a/src/driver/amdxdna/aie2_message.c
+++ b/src/driver/amdxdna/aie2_message.c
@@ -59,11 +59,11 @@ static int aie2_send_mgmt_msg_wait(struct amdxdna_dev_hdl *ndev,

 	drm_WARN_ON(&xdna->ddev, !mutex_is_locked(&ndev->aie2_lock));
 	ret = xdna_send_msg_wait(xdna, ndev->mgmt_chann, msg);
-	if (ret == -ETIME) {
-		xdna_mailbox_stop_channel(ndev->mgmt_chann);
-		xdna_mailbox_destroy_channel(ndev->mgmt_chann);
-		ndev->mgmt_chann = NULL;
-	}
+	/*
+	 * Previously: on -ETIME we destroyed mgmt_chann to defend against
+	 * protocol corruption. That converts a single-opcode hang into a
+	 * device-wide outage. See finding doc 2026-05-07; the firmware-side
+	 * AIE_RW_ACCESS read on memtile is the only known opcode that hangs.
+	 */

 	if (!ret && *hdl->status != AIE2_STATUS_SUCCESS) {
 		XDNA_ERR(xdna, "command opcode 0x%x failed, status 0x%x",
